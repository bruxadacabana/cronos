# Cronos v1.3 — dependências

# Core UI
PyQt6>=6.4.0

# Feeds RSS
feedparser>=6.0

# HTTP
httpx>=0.24

# Scraping — hierárquico (melhor disponível é usado automaticamente)
trafilatura>=1.6          # 1º: melhor extrator, retorna HTML estruturado
readability-lxml>=0.8     # 2º: extrator DOM clássico
goose3>=3.1               # 3º: alta precisão de extração (novo)
beautifulsoup4>=4.12      # 4º: fallback manual

# Formatação de conteúdo
markdownify>=0.13         # converte HTML extraído em Markdown estruturado
# markdown>=3.6           # opcional: renderiza Markdown via stdlib (mais rico)

# Tradução
deep-translator>=1.11

# Redes sociais
praw>=7.7                           # Reddit
atproto>=0.0.46                     # Bluesky
twscrape>=0.12                      # Twitter/X (desativado por padrão)
google-api-python-client>=2.0       # YouTube
